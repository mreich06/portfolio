import styled from 'styled-components';
import { motion } from 'framer-motion';

export const Border = styled(motion.li)`
  width: 1000px;
  height: 20px;
  background: linear-gradient(90deg, #b621fe, #1fd1f9);
  border-radius: 20px;
  flex: 1;
`;
